---
title: 자동 시작
description: 시스템 시작 시 앱을 자동으로 실행합니다.
plugin: autostart
i18nReady: true
---

import PluginLinks from '@components/PluginLinks.astro';
import Compatibility from '@components/plugins/Compatibility.astro';

import { Tabs, TabItem, Steps } from '@astrojs/starlight/components';
import CommandTabs from '@components/CommandTabs.astro';
import PluginPermissions from '@components/PluginPermissions.astro';

<PluginLinks plugin={frontmatter.plugin} />

시스템 시작 시 애플리케이션을 자동으로 실행합니다.

## 지원되는 플랫폼

<Compatibility plugin={frontmatter.plugin} />

## 설정

자동 시작 플러그인을 설치하여 시작하십시오.

<Tabs>
  <TabItem label="자동">

    프로젝트의 패키지 관리자를 사용하여 의존성을 추가하십시오:

    {' '}

    <CommandTabs
      npm="npm run tauri add autostart"
      yarn="yarn run tauri add autostart"
      pnpm="pnpm tauri add autostart"
      deno="deno task tauri add autostart"
      bun="bun tauri add autostart"
      cargo="cargo tauri add autostart"
    />

  </TabItem>
    <TabItem label="수동">
      <Steps>

        1. `src-tauri` 폴더에서 다음 명령을 실행하여 `Cargo.toml`의 프로젝트 의존성에 플러그인을 추가하십시오:

            ```sh frame=none
            cargo add tauri-plugin-autostart --target 'cfg(any(target_os = "macos", windows, target_os = "linux"))'
            ```

        2.  `lib.rs`를 수정하여 플러그인을 초기화하십시오:

            ```rust title="src-tauri/src/lib.rs" ins={5-6}
            #[cfg_attr(mobile, tauri::mobile_entry_point)]
            pub fn run() {
                tauri::Builder::default()
                    .setup(|app| {
                        #[cfg(desktop)]
                        app.handle().plugin(tauri_plugin_autostart::init(tauri_plugin_autostart::MacosLauncher::LaunchAgent, Some(vec!["--flag1", "--flag2"]) /* 앱에 전달할 임의의 수의 인자 */));
                        Ok(())
                    })
                    .run(tauri::generate_context!())
                    .expect("error while running tauri application");
            }
            ```

        3.  원하는 JavaScript 패키지 관리자를 사용하여 JavaScript 게스트 바인딩을 설치할 수 있습니다:

            <CommandTabs
                npm="npm install @tauri-apps/plugin-autostart"
                yarn="yarn add @tauri-apps/plugin-autostart"
                pnpm="pnpm add @tauri-apps/plugin-autostart"
                deno="deno add npm:@tauri-apps/plugin-autostart"
                bun="bun add @tauri-apps/plugin-autostart"
            />

      </Steps>
    </TabItem>

</Tabs>

## 사용법

자동 시작 플러그인은 JavaScript와 Rust 모두에서 사용할 수 있습니다.

<Tabs syncKey="lang">
  <TabItem label="JavaScript">

```javascript
import { enable, isEnabled, disable } from '@tauri-apps/plugin-autostart';
// `"withGlobalTauri": true`를 사용할 때, 다음을 사용할 수 있습니다:
// const { enable, isEnabled, disable } = window.__TAURI__.autostart;

// 자동 시작 활성화
await enable();
// 활성화 상태 확인
console.log(`registered for autostart? ${await isEnabled()}`);
// 자동 시작 비활성화
disable();
```

  </TabItem>
  <TabItem label="Rust">

```rust
#[cfg_attr(mobile, tauri::mobile_entry_point)]
pub fn run() {
    tauri::Builder::default()
        .setup(|app| {
            #[cfg(desktop)]
            {
                use tauri_plugin_autostart::MacosLauncher;
                use tauri_plugin_autostart::ManagerExt;

                app.handle().plugin(tauri_plugin_autostart::init(
                    MacosLauncher::LaunchAgent,
                    Some(vec!["--flag1", "--flag2"]),
                ));

                // 자동 시작 관리자 가져오기
                let autostart_manager = app.autolaunch();
                // 자동 시작 활성화
                let _ = autostart_manager.enable();
                // 활성화 상태 확인
                println!("registered for autostart? {}", autostart_manager.is_enabled().unwrap());
                // 자동 시작 비활성화
                let _ = autostart_manager.disable();
            }
            Ok(())
        })
        .run(tauri::generate_context!())
        .expect("error while running tauri application");
}
```

  </TabItem>
</Tabs>

## 권한

기본적으로 잠재적으로 위험한 모든 플러그인 명령과 범위는 차단되어 액세스할 수 없습니다. 이를 활성화하려면 `capabilities` 구성에서 권한을 수정해야 합니다.

플러그인 권한을 사용하는 방법에 대한 자세한 내용은 [기능 개요](/security/capabilities/) 및 [단계별 가이드](/learn/security/using-plugin-permissions/)를 참조하십시오.

```json title="src-tauri/capabilities/default.json"
{
  "permissions": [
    ...,
    "autostart:allow-enable",
    "autostart:allow-disable",
    "autostart:allow-is-enabled"
  ]
}
```

<PluginPermissions plugin={frontmatter.plugin} />
